# FSAD — AI-List-Assist Operational Ledger

State machine: proposals/ → approved/ → archive/

Records: listing workflows, valuation jobs, consignment transactions.

Schema: id, created_at, state, owner, tags (YAML frontmatter in each file).

See: [docs/governance/fsad-architecture.md](../docs/governance/fsad-architecture.md)